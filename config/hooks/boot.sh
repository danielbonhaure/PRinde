#!/bin/sh

# Script called every time the software is starting.
# Could be used to create SSH tunnels for database connections.
# Absolute path to this script, e.g. /home/user/bin/foo.sh
#SCRIPT=$(readlink -f "$0")
# Absolute path this script is in, thus /home/user/bin
#SCRIPTPATH=$(dirname "$SCRIPT")

#sudo mount -t smbfs 192.168.1.115/usmfpts/descargas/series_combinadas "$SCRIPTPATH/../.tmp/wth"

