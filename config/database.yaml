# Databases configuration.

weather_db :
    type: 'postgresql'
    host : '192.168.1.115'
    db_name : 'crc_ssa'
#    user : 'crcssa_user'
    user : 'postgres'
    # password : '' # Uncomment to specify here, otherwise, create a text file inside the config/pwd directory with the
                    # same name as the database user and write it there with ".pwd" extension.
                    # Files inside config/pwd are kept out git repositories.
    port : 5432

#weather_db :
#    type: 'postgresql'
#    host : 'localhost'
#    db_name : 'crcssa'
#    user : 'postgres'
#    password : 'rinde' # Uncomment to specify here, otherwise, create a text file inside the config/pwd directory with the
#                    # same name as the database user and write it there with ".pwd" extension.
#                    # Files inside config/pwd are kept out git repositories.
#    port : 5432

yield_db:
    type: 'mongodb'
    host: 'localhost'
    db_name: 'Rinde'
    port: 27017

yield_sync_db:
    type: 'mongodb'
    host: 'localhost'
    db_name: 'RindeSync'
    port: 27017